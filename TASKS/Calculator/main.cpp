#include "calculator.h"

int main()
{
    Calculator calc;
    calc.add(5 ,3);
    calc.subtract(10 , 7);
    calc.multiply(30, 10);
    calc.divide(20 , 10);
    calc.modulus(5 , 2);
    return 0;
}
